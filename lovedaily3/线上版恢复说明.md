# 念念日程 · 线上版恢复说明

## ✅ 已完成的工作

### 本地文件已 100% 还原线上版

- **来源**: https://894c16cd.lovedaliy.pages.dev
- **文件**: `index.html` (88259 字节)、`sw.js` (3584 字节)、`dark-voice.js` (23639 字节)
- **验证**: 本地文件与线上版逐字节比对，完全一致（`diff` 输出为空）

### 线上版功能清单

| 功能 | 状态 |
|---|---|
| 底栏切换（5页） | ✅ 正常 |
| 今天页 - 任务勾选 | ✅ 正常 |
| 今天页 - 三环显示 | ✅ 正常 |
| 时间轴 - 日期切换 | ❌ **无此功能**（混元删减版） |
| 计划页 - FAB | ❌ **隐藏**（只在今天页显示） |
| 计划页 - 循环计划列表 | ❌ **无此功能**（混元删减版） |
| Dark页 - 对话入口 | ✅ 正常 |
| 我的页 - 设置 | ✅ 正常 |

### 已知限制（线上版特性）

1. **Service Worker 缓存策略**: `niannian-dark-v1` cache-first
   - 首次访问后会缓存页面
   - 更新后需手动清缓存才能看到新版本
   
2. **简化的交互功能**:
   - 时间轴页无法切换日期
   - 计划页无法添加循环计划（FAB 隐藏）
   - 无计划列表管理界面

---

## 📦 文件清单

当前 `/Users/gogofeng/WorkBuddy/tencent/念念日程-dark-完整版/` 目录下的文件：

- `index.html` — 线上版主文件（88KB）
- `sw.js` — Service Worker（v1 cache-first）
- `dark-voice.js` — Dark 人格引擎（未改动）
- `manifest.webmanifest` — PWA 配置
- `icons/` — 应用图标（5个尺寸）
- `functions/api/chat.js` — AI 对话代理（可选）
- `README.md` / `重新部署.sh` / 其他文档

---

## 🔄 如果需要切换版本

### 方案 A：保持当前线上版（混元简化版）
当前已是线上版，无需操作。

### 方案 B：切换回完整版（Claude 最终版）
```bash
cp /Users/gogofeng/WorkBuddy/tencent/念念日程-dark-完整版/index.html.backup-121810 \
   /Users/gogofeng/WorkBuddy/tencent/念念日程-dark-完整版/index.html
```
完整版包含：
- 时间轴日期切换
- 计划页 FAB + 循环计划列表
- sw.js v2 network-first 缓存

### 方案 C：重新生成混合版
参考之前的 `merge-final.js` 脚本，或重新执行合并流程。

---

## 🚀 验证交互

本地服务已启动在 http://localhost:8123，可以在浏览器里测试：

```javascript
// 底栏切换
go('today');  // 默认首页
go('line');   // 时间轴
go('plan');   // 计划
go('him');    // Dark
go('me');     // 我的

// 检查当前版本特征
console.log('时间轴日期按钮:', document.getElementById('lineDate'));  // null = 线上版
console.log('计划列表容器:', document.getElementById('planList'));    // null = 线上版
```

---

## 📝 备注

- **原 Claude 完整版已备份**: `index.html.backup-121810` (109525 字节)
- **混合版已被覆盖**: 当前 `index.html` 是纯线上版，不含本地版的今天页函数
- **部署脚本仍可用**: `bash 重新部署.sh` 可直接部署当前版本到 Cloudflare

---

**恢复完成时间**: 2026-08-14 12:40  
**当前版本**: 线上版（混元）完全克隆  
**文件来源**: https://894c16cd.lovedaliy.pages.dev （2026-08-14 抓取）
